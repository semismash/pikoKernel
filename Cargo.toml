[package]
name = "pikoKernel"
version = "0.1.0"
edition = "2024"

[lib]
name = "pikoKernel"
crate-type = ["staticlib"]
path = "src/lib.rs"

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"